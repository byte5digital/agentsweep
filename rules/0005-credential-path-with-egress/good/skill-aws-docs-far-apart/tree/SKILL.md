---
name: aws-helper
description: Help with AWS CLI profiles.
---

Profiles live in ~/.aws/credentials; never print that file. Use `aws configure list` to inspect the active profile.

Section 1: explain the difference between IAM roles and users, with an example of assuming a role via STS and the environment variables the CLI reads.

Section 2: explain the difference between IAM roles and users, with an example of assuming a role via STS and the environment variables the CLI reads.

Section 3: explain the difference between IAM roles and users, with an example of assuming a role via STS and the environment variables the CLI reads.

Section 4: explain the difference between IAM roles and users, with an example of assuming a role via STS and the environment variables the CLI reads.

Section 5: explain the difference between IAM roles and users, with an example of assuming a role via STS and the environment variables the CLI reads.

Section 6: explain the difference between IAM roles and users, with an example of assuming a role via STS and the environment variables the CLI reads.

Section 7: explain the difference between IAM roles and users, with an example of assuming a role via STS and the environment variables the CLI reads.

Section 8: explain the difference between IAM roles and users, with an example of assuming a role via STS and the environment variables the CLI reads.

Section 9: explain the difference between IAM roles and users, with an example of assuming a role via STS and the environment variables the CLI reads.

Section 10: explain the difference between IAM roles and users, with an example of assuming a role via STS and the environment variables the CLI reads.

Section 11: explain the difference between IAM roles and users, with an example of assuming a role via STS and the environment variables the CLI reads.

Section 12: explain the difference between IAM roles and users, with an example of assuming a role via STS and the environment variables the CLI reads.

To fetch the pricing sheet run:

    curl -fsSL -o pricing.json https://pricing.example.com/index.json
